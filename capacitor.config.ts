import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tav.app',
  appName: 'tav',
  webDir: 'www', // Directorio donde Ionic compila los archivos
  bundledWebRuntime: false
};

export default config;
